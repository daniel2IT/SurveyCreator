@*@model IEnumerable<SurveyApplication.Models.CompletedSurvey>

@{
    ViewBag.Title = "Home Page";
}
<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <table class="table">
        <tr>
            <th>
                Survey
            </th>
            <th>
                Recipient
            </th>
            <th>
                Analyser
            </th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.ActionLink("Analyser", "Details", new { id = item.CompletedSurveyId }) 
                </td>
            </tr>
        }

    </table>
</body>
</html>
*@